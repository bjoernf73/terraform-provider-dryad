# Organizational unit example

This example shows the initial `dryad_organizational_unit` resource using WinRM.

## Notes

- Adjust `path` to match your domain naming convention.
- Provide credentials and Kerberos realm values suitable for your lab or target domain.
- Run this only against a disposable environment until you are comfortable with the provider behavior.
